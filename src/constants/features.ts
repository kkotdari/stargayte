// 기능 토글 — 배포는 하되 아직 사용자에게 열지 않는 기능을 한 곳에서 켜고 끈다.

// 대결 요청(너 나와 최상단 요청 코너 + 언급 알림 인박스)을 임시로 숨긴다(요청: "너 나와
// 대결 요청 기능 숨김(추후 오픈)"). 다시 열 때 이 값만 true로 바꾸면 진입점(요청 코너)과
// 언급 알림 팝업이 함께 되살아난다. 백엔드/데이터는 그대로 두고 화면 노출만 가린다.
export const MATCH_REQUEST_ENABLED = false;
